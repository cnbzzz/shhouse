<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.jufangbao.house.shhouse.mapper.TResHouseLabelMapper">
  <resultMap id="BaseResultMap" type="cn.jufangbao.house.shhouse.model.TResHouseLabel">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="oid" jdbcType="VARCHAR" property="oid" />
    <result column="hoid" jdbcType="VARCHAR" property="hoid" />
    <result column="hlabel" jdbcType="VARCHAR" property="hlabel" />
    <result column="status" jdbcType="INTEGER" property="status" />
  </resultMap>

  <select id="selectHouseLabel" parameterType="Long" resultType="cn.jufangbao.house.shhouse.dto.HouseLabelDto">
    SELECT
        t1.*, t2.dictid,
        t2.dictname
    FROM
        t_res_house_label t1
    LEFT JOIN t_sa_dict_item t2 ON t1.hlabel = t2.dictid
    AND t2.groupid = 'HOUSE_LABEL'
    AND t2. STATUS = 1
    WHERE
        t1. STATUS = 1
    AND t1.hoid = #{hoid,jdbcType=BIGINT}
  </select>
</mapper>